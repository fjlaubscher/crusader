import React from 'react';

// components
import Alert from '../../components/alert';
import Layout from '../../components/layout';

const CreateList = () => {
  return (
    <Layout title="List Builder">
      <Alert variant="warning">Coming soon!</Alert>
    </Layout>
  );
};

export default CreateList;
